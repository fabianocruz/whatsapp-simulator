# No trailing slash: with node_modules symlinked to local disk (see CONTRIBUTING),
# a directory-only pattern would leave the symlink itself untracked-but-visible.
node_modules
dist/
.next/
out/
coverage/
*.tsbuildinfo
.DS_Store
.env
.env.local
